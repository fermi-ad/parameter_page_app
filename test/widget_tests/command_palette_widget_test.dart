import 'package:flutter_test/flutter_test.dart';

void main() {
  group("CommandPaletteWidget", () {
    testWidgets('No data from stream yet, displays Loading...',
        (WidgetTester tester) async {});
  });
}
